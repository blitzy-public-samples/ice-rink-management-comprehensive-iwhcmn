import App from './App';

// Register the main App component with React Native
AppRegistry.registerComponent('IceRinkBookingApp', () => App);

// Human tasks:
// TODO: Ensure the app name 'IceRinkBookingApp' matches the name in app.json and other configuration files
// TODO: Configure any necessary polyfills or global error handlers before registering the app